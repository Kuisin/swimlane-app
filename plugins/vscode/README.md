# VS Code extension output

Built `.vsix` files are written here by:

```bash
npm run package:extension
```

Example: `vscode-kai-swimlane-0.1.0.vsix`

Install:

```bash
cursor --install-extension plugins/vscode/vscode-kai-swimlane-0.1.0.vsix
```

Source lives in `extensions/vscode-kai-swimlane/`.
